---
name: webgpu-threejs-tsl
description: Remote-backed LeeWay binding for WebGPU, Three.js WebGPU renderer, TSL shaders, GPU effects, examples, templates, and implementation guidance.
---
# WebGPU / Three.js / TSL
Canonical source: `dgreenheck/webgpu-claude-skill` → `skills/webgpu-threejs-tsl` on `main`.

Prefer synchronized `upstream/SKILL.md`; load `REFERENCE.md`, docs, examples, or templates only when the selected workflow requires them. Validate browser/runtime WebGPU support before claiming execution. Never claim shader compilation, GPU rendering, frame rate, or device compatibility without an executed test.

State while `upstream/` is absent: `REMOTE_CANONICAL`.
