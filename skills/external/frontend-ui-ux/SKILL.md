---
name: frontend-ui-ux
description: Remote-backed LeeWay binding to the current frontend router descended from the Frontend UI UX skill in code-yeongyu/oh-my-openagent.
---
# Frontend UI UX
Canonical source: `code-yeongyu/oh-my-openagent` → `packages/shared-skills/skills/frontend` on `dev`.

The historical `frontend-ui-ux` naming evolved into the current `frontend` router. Prefer synchronized `upstream/SKILL.md`; otherwise retrieve that canonical router and its references through authorized GitHub/network access. Preserve this lineage rather than inventing a stale file path.

State while `upstream/` is absent: `REMOTE_CANONICAL`.