---
name: shadcn
description: Remote-backed LeeWay binding to the official shadcn UI skill for component registries, composition, theming, CLI usage, and project implementation.
---
# shadcn UI
Canonical source: `shadcn-ui/ui` → `skills/shadcn` on `main`.

Prefer synchronized `upstream/SKILL.md`. If absent, retrieve the official skill and required sibling references from GitHub. Follow the actual shadcn project conventions rather than recreating components blindly. Preserve LeeWay/Creator authority and verify generated code through the project build/tests when available.

State while `upstream/` is absent: `REMOTE_CANONICAL`.