---
name: mobile-app-ui-design
description: Remote-backed binding to ceorkm Mobile App UI Design for mobile product layout, interaction, visual hierarchy, and platform-aware interface design.
---
# Mobile App UI Design
Canonical source: `ceorkm/mobile-app-ui-design` repository root on `main`.

Prefer synchronized `upstream/SKILL.md` and its reference files. If absent, retrieve the canonical root `SKILL.md` and required references through authorized GitHub/network access. Apply platform constraints explicitly and verify responsive/device states when tooling exists.

State while `upstream/` is absent: `REMOTE_CANONICAL`.